# macOS
.DS_Store

# Claude Code — per-developer state
.claude/

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Xcode — build artifacts
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# CocoaPods / Carthage (unused, but ignore if ever added)
Pods/
Carthage/Build/

# Scratch screenshot paste target used mid-session
/image.png

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Misc
*.log

# Personal / non-source working files (kept out of the public repo)
app-store/
design/
*.docx

# Local-only research and prototype tooling (plant classifier exploration,
# ha-mcp experiments, etc.) — kept out of the OSS repo entirely per the
# "maximally clean" stance. See tools/plant-classifier-training/FINDINGS.md.
tools/
